/**
 * Database module - Production-ready repository pattern for R2 storage
 *
 * This module provides type-safe CRUD operations with:
 * - Repository pattern for each entity type
 * - Batch operations support
 * - Query helpers with filters
 * - Transaction-like batch writes
 */

export * from './types';
export * from './storage';
export * from './repositories';

